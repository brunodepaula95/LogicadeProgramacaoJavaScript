primeiro = parseInt(prompt("Digite o primeiro numero:"));
segundo = parseInt(prompt("Digite o segundo número: "));

console.log(soma(primeiro,segundo));

function soma(primeiro, segundo){
    return primeiro + segundo;
}